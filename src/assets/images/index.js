const images = {
    logo: require('@/assets/images/logo.svg').default, // đối với ảnh đuôi svg thì cần default. Ngược lại thì không cần
    missingImg: require('@/assets/images/missing-img.png'),
};

export default images;
